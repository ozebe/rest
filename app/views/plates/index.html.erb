<p id="notice"><%= notice %></p>

<h1>Pratos</h1>

<table>
  <thead>
    <tr>
      <th>Descrição</th>
      <th>Preço</th>
      <th>Tempo de preparo(minutos)</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @plates.each do |plate|%>
      <tr>
        <td><%= plate.description %></td>
        <td><%= plate.price %></td>
        <td><%= plate.timeprep %></td>
        <td><%= link_to 'Visualizar', plate %></td>
        <td><%= link_to 'Editar', edit_plate_path(plate) %></td>
        <td><%= link_to 'Apagar', plate, method: :delete, data: { confirm: 'Você tem certeza?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<h3><%= link_to 'Cadastrar novo prato', new_plate_path %></h3>
<h1><%= link_to 'Home', home_index_path%></h1>
